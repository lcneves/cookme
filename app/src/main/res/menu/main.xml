<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_search"
        android:title="@string/action_search"
        android:showAsAction="always|withText"
        android:onClick="clickSearch">
<!--        <menu>
            <item android:id="@+id/action_name_search"
                android:title="@string/action_name_search"
                android:onClick="clickSearchByName"/>

            <item android:id="@+id/action_ingredients_search"
                android:title="@string/action_ingredients_search"
                android:onClick="searchRecipes"/>

            <item android:id="@+id/action_composite_search"
                android:title="@string/action_composite_search"
                android:onClick="clickSearchComposite"/>
        </menu>-->
    </item>
    <item android:id="@+id/action_clearDb"
        android:title="@string/action_clearDb"
        android:orderInCategory="100"
        android:showAsAction="never" />
    <item android:id="@+id/action_about"
    android:title="@string/action_about"
    android:orderInCategory="200"
    android:showAsAction="never"
        android:onClick="clickAboutMenuMain"/>
</menu>


    <!--android:icon="@drawable/ic_action_search"-->